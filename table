extends Sprite2D
var speed = 400
var angular_speed = 20
var moving = true

func _init():

	print("Hello, world!")

	
func _process(delta):
	
	if moving:
		rotation += angular_speed * delta
		angular_speed -= 0.01
	if angular_speed == 0:
		moving = false
	
